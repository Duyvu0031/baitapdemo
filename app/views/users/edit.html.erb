<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Account</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f4f6f9;
      font-family: 'Arial', sans-serif;
    }
    .container {
      max-width: 500px;
      background-color: #ffffff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      margin-top: 100px;
    }
    h1 {
      font-size: 2rem;
      font-weight: 700;
      color: #333;
      margin-bottom: 20px;
    }
    .form-label {
      font-size: 1.1rem;
      font-weight: 600;
      color: #555;
    }
    .form-control {
      border-radius: 10px;
      padding: 10px;
      font-size: 1rem;
      transition: all 0.3s ease;
    }
    .form-control:focus {
      border-color: #007bff;
      box-shadow: 0 0 10px rgba(0, 123, 255, 0.2);
    }
    .btn-primary {
      background-color: #28a745; /* Changed color to green for Save Changes */
      border-color: #28a745;
      padding: 12px 20px;
      width: 100%;
      border-radius: 50px;
      font-size: 1.1rem;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #218838; /* Darker green for hover */
      transform: scale(1.05);
    }
    .alert {
      background-color: #f8d7da;
      color: #721c24;
      border-radius: 10px;
      padding: 15px;
      margin-bottom: 20px;
    }
    .btn-link {
      color: #007bff;
      text-decoration: none;
    }
    .btn-link:hover {
      text-decoration: underline;
    }
    .form-group {
      margin-bottom: 20px;
    }
    .container small {
      display: block;
      margin-top: 10px;
      color: #777;
    }
  </style>
</head>
<body>

<div class="container">
  <h1 class="text-center mb-4">Chỉnh Sửa Tài Khoản</h1>

  <% if @user.errors.any? %>
    <div class="alert alert-danger">
      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with model: @user, local: true do |form| %>
    <div class="mb-3">
      <%= form.label :email_address, "Tên Tài Khoản Email", class: "form-label" %>
      <%= form.email_field :email_address, class: "form-control", readonly: true %> <!-- Email không thể chỉnh sửa -->
      <small>Không thể thay đổi email của bạn.</small>
    </div>

    <div class="mb-3">
      <%= form.label :password, "Mật khẩu mới", class: "form-label" %>
      <%= form.password_field :password, class: "form-control" %>
      <small>Để thay đổi mật khẩu, vui lòng nhập mật khẩu mới.</small>
    </div>

    <div class="mb-3">
      <%= form.label :password_confirmation, "Xác nhận mật khẩu mới", class: "form-label" %>
      <%= form.password_field :password_confirmation, class: "form-control" %>
    </div>

    <div class="text-center">
      <%= form.submit "Lưu Thay Đổi", class: "btn btn-primary" %>
    </div>
  <% end %>

</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
