<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh Sách Tài Khoản</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    h1 {
      font-weight: bold;
      color: #343a40;
    }

    .table thead th {
      background-color: #212529;
      color: white;
      font-weight: bold;
    }

    .btn-custom {
      font-size: 0.9rem;
      font-weight: 600;
      padding: 8px 12px;
      border-radius: 25px;
      transition: all 0.3s ease;
    }

    .btn-edit {
      background-color: #ffc107;
      color: #fff;
      border: none;
    }

    .btn-edit:hover {
      background-color: #e0a800;
      color: white;
    }

    .btn-delete {
      background-color: #dc3545;
      color: #fff;
      border: none;
    }

    .btn-delete:hover {
      background-color: #c82333;
      color: white;
    }

    .table-responsive {
      margin-top: 20px;
    }

    .table td, .table th {
      vertical-align: middle;
    }
  </style>
</head>
<body>
  <div class="container my-4">
    <h1 class="text-center">Danh Sách Tài Khoản</h1>

    <div class="table-responsive">
      <table class="table table-bordered table-striped text-center">
        <thead class="thead-dark">
          <tr>
            <th>Tên Tài Khoản Email</th>
            <th>Hành Động</th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.email_address %></td>
              <td>
                <!-- Nút Sửa chỉ hiển thị cho admin và không phải người dùng hiện tại -->
                <% if current_user.email_address == "duy0031@gmail.com" && user != current_user %>
                  <%= link_to "Sửa", edit_user_path(user), class: "btn btn-custom btn-edit mx-2" %>
                <% end %>

                
                <% if current_user.email_address == "duy0031@gmail.com" && user != current_user %>
                  <%= link_to "Xóa", user_path(user), 
                              method: :delete, 
                              data: { confirm: "Are you sure you want to delete this account?" }, 
                              class: "btn btn-custom btn-delete" %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
