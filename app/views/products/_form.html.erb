<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Form</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f4f6f9;
      font-family: 'Arial', sans-serif;
    }
    .container {
      max-width: 700px;
      background-color: #ffffff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      margin-top: 50px;
    }
    h1 {
      font-size: 2rem;
      font-weight: 700;
      color: #333;
      text-align: center;
      margin-bottom: 30px;
    }
    .form-label {
      font-size: 1.1rem;
      font-weight: 600;
      color: #555;
    }
    .form-control, .form-file, .form-submit {
      border-radius: 10px;
      padding: 12px;
      font-size: 1rem;
      transition: all 0.3s ease;
    }
    .form-control:focus, .form-file:focus, .form-submit:hover {
      border-color: #007bff;
      box-shadow: 0 0 10px rgba(0, 123, 255, 0.2);
    }
    .form-submit {
      background-color: #007bff;
      color: white;
      font-size: 1.2rem;
      width: 100%;
      border-radius: 50px;
      padding: 14px;
      border: none;
      cursor: pointer;
    }
    .form-submit:hover {
      background-color: #0056b3;
      transform: scale(1.05);
    }
    .file-label {
      margin-top: 10px;
      margin-bottom: 10px;
      font-size: 1.1rem;
      font-weight: 600;
      color: #555;
    }
    .rich-text-area {
      width: 100%;
      height: 200px;
      border-radius: 10px;
      padding: 12px;
      font-size: 1rem;
      transition: all 0.3s ease;
    }
    .rich-text-area:focus {
      border-color: #007bff;
      box-shadow: 0 0 10px rgba(0, 123, 255, 0.2);
    }
    .form-group {
      margin-bottom: 20px;
    }
    .alert {
      background-color: #f8d7da;
      color: #721c24;
      border-radius: 10px;
      padding: 15px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>

<div class="container">
  <h1>Add a New Product</h1>

  <% if product.errors.any? %>
    <div class="alert alert-danger">
      <h4>There were errors with your submission:</h4>
      <ul>
        <% product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with model: product, local: true do |form| %>

    <div class="form-group">
      <%= form.label :name, class: "form-label" %>
      <%= form.text_field :name, class: "form-control", placeholder: "Enter product name", required: true %>
    </div>

    <div class="form-group">
      <%= form.label :description, class: "form-label" %>
      <%= form.rich_text_area :description, class: "rich-text-area", placeholder: "Describe the product", required: true %>
    </div>

    <div class="form-group">
      <%= form.label :featured_image, class: "file-label" %>
      <%= form.file_field :featured_image, accept: "image/*", class: "form-file" %>
    </div>

    <div class="form-group">
      <%= form.label :inventory_count, class: "form-label" %>
      <%= form.number_field :inventory_count, class: "form-control", placeholder: "Enter the inventory count", required: true %>
    </div>

    <div class="form-group">
      <%= form.submit "Save Product", class: "form-submit" %>
    </div>

  <% end %>

</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
