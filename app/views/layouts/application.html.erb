<!DOCTYPE html>
<html lang="en">
<head>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <title><%= content_for(:title) || "Tùng Duy" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

<link rel="icon" href="/apple.png" type="image/png">
<link rel="icon" href="/carrot.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/carrot.png">
  <!-- Bootstrap CSS -->
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag "trix" %>
  <%= javascript_include_tag "trix" %>

  <!-- FontAwesome for icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

  <style>
  body {
    background-color: #f4f6f9;
    font-family: 'Roboto', sans-serif;
    color: black; 
  }
  .navbar-text {
    color: orange !important; /* Đặt màu chữ thành cam */
  }
    .navbar {
      border-bottom: 2px solid #f1f1f1;
    }
    .navbar-brand {
      font-weight: 600;
      font-size: 1.25rem;
    }
    .nav-link {
      font-weight: 500;
      padding: 10px 20px;
      transition: all 0.3s ease;
    }
    .nav-link:hover {
      background-color: #f1f1f1;
      border-radius: 5px;
    }

    .btn-logout {
      font-weight: 500;
      padding: 10px 20px;
      background-color: transparent;
      border: 2px solid #dc3545;
      color: #dc3545;
      border-radius: 5px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
    }

    .btn-logout:hover {
      background-color: #dc3545;
      color: white;
      border-color: #dc3545;
    }

    .btn-logout i {
      margin-right: 8px;
    }

    .container {
      margin-top: 30px;
    }
    .navbar-text,
.navbar-brand {
  color: black !important; 
}
    
.nav-link, 
.btn-logout, 
footer, 
footer p, 
.alert {
  color: black !important;
}
    footer {
      background-color: #212529;
      padding: 20px;
      color: #fff;
      font-size: 0.9rem;
    }
    footer, footer p {
      margin: 0;
  color: white !important; /* Đặt màu trắng và dùng !important để ưu tiên */
}
  </style>
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <%= link_to "Trang Chủ", all_products_products_path, class: "navbar-brand" %>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <!-- Dòng chào mừng -->
          <li class="nav-item">
            <span class="navbar-text">
    Chào mừng, <%= current_user.email_address || "Người dùng" %>!
  </span>
          </li>

          <!-- Nút Tài Khoản -->
          <li class="nav-item">
            <%= link_to "Tài Khoản", root_path, class: "nav-link" %>
          </li>

          <!-- Nút Đăng Xuất -->
          <li class="nav-item">
            <%= button_to session_path, method: :delete, class: "nav-link btn-logout" do %>
              <i class="fas fa-sign-out-alt"></i> Đăng Xuất
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


  <!-- Notice -->
  <div class="container">
    <div class="alert alert-info">
      <%= notice %>
    </div>
  </div>

  <!-- Main content -->
  <main>
    <div class="container">
      <%= yield %>
    </div>
  </main>

  <!-- Footer -->
  <footer class="text-center">
    <p>&copy; 2025 Thiết Kế Và Phát Triển Webstite Mã Nguồn Mở. Bài Cuối Kỳ [Tùng Duy - DH21LT].</p>
  </footer>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
