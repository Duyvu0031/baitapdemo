<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <title><%= content_for(:title) || "Mysite" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <!-- Favicon -->
  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <!-- Bootstrap CSS -->
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag "trix" %>
  <%= javascript_include_tag "trix" %>
  
</head>

<body class="bg-light">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <%= link_to "Home", root_path, class: "navbar-brand" %>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= link_to "Xem sản phẩm", product_order_path(@product, @order), class: "nav-link" if @product && @order %>
          </li>

          <% if current_user %>
            <li class="nav-item">
              <%= button_to "Log out", session_path, method: :delete, class: "nav-link btn btn-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Notice -->
  <div class="container mt-4">
    <div class="alert alert-info">
      <%= notice %>
    </div>
  </div>

  <!-- Main content -->
  <main>
    <div class="container">
      <%= yield %>
    </div>
  </main>

  <!-- Footer (Optional) -->
  <footer class="bg-dark text-white text-center py-4 mt-5">
    <p>&copy; 2025 Thiết Kế Và Phát Triển Webstite Mã Nguồn Mở. Bài Cuối KỲ[Tùng Duy - DH21LT].</p>
  </footer>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
